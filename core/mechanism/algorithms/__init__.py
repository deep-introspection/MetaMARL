"""Concrete regulatory mechanisms.

One module per mechanism: ``quota`` (action channel), ``subsidy`` and
``penalty`` (reward channel) and ``social_influence`` (observation channel).
This ``__init__`` re-exports nothing; import from the submodules directly.
"""
