"""Bilevel fishery example: an ES regulator over a quota + subsidy stack, APPO fishers inside.

The package holds the regulated environment (``regulated_env``), the
regulator environment that scores mechanism candidates (``regulator_env``),
the fitness context exchanged through the World actor (``contexts``), the
metric schema and the reporting queries, and ``debug`` as the runnable entry
point. Nothing is re-exported from here.
"""
